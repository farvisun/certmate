# Moved

This page was the client-certificate launch write-up, sitting under a
filename that made it look like the documentation index. Its content now
lives in the [client-certificate guide](./guide.md), and the index is
[README.md](./README.md) — which is also what GitHub renders when you browse
to `docs/`.

Kept as a stub so existing deep links do not 404.
